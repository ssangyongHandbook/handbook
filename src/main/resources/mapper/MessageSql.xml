<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTDMapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sns.handbook.mapper.MessageMapperInter">
	<select id="selectAllChatMemeber" parameterType="String" resultType="MessageDto">
		select distinct mess_group,sender_num,receiver_num from message where sender_num=#{user_num} or receiver_num=#{user_num};
	</select>
	<select id="selectRecentMessage" parameterType="Map" resultType="MessageDto">
		select mess_content,mess_writeday from message where (sender_num=#{user_num} and receiver_num=#{other}) or (sender_num=#{other} and receiver_num=#{user_num}) order by mess_writeday desc limit 1
	</select>
</mapper>